package com.getcatch.android.ui

/**
 * Recommended size options for the [CatchLogo] widget. For use at a custom size, you
 * can use the [CatchLogoSize.FILL] within a container of the size of your choosing.
 */
public enum class CatchLogoSize {
    SMALL,
    MEDIUM,
    FILL;
}
